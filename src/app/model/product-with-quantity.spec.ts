import { ProductWithQuantity } from './product-with-quantity';

describe('ProductWithQuantity', () => {
  it('should create an instance', () => {
    expect(new ProductWithQuantity()).toBeTruthy();
  });
});
